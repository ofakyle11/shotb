GOLDEN READY — GRABBED VIA LOCAL GIT + MCP.

Local authoritative: C:\Users\kylefrancis\SHOTBREAK-FINAL-FLOW-UPDATE.html (463273 bytes, 7375 lines)

Synced deploy copy: app.html (identical after Copy-Item)

Local git clone at C:\Users\kylefrancis\Shotbreak now has:
- Commit 70226b8: full golden app.html 463273b (replaced the ~87k stub)
- Commit 16aaa5f: synced detailed DO_THIS_NOW / MINIMAL / POST_CHECKLIST / GOLDEN_READY + netlify/functions from root golden source

MCP + git used to prepare. User to `cd Shotbreak; git push` in normal terminal (creds available there) to land 70226b8 + 16aaa5f on remote.

Contains exactly the verified features:
- 4 tabs only (Script | Characters | Generate (with Settings) | Edit/Upscale)
- Per-shot: LOCK (class lock-btn, toggleShotMinimize), 8 jot dropdowns, mhParseBox + 🔒 Relock, big prompt, char photos + loc selects + Use global
- 82 agents labeled in .agent-contrib (prompt-writer: etc.)
- BROAD_* + "full choice — no client blocks"
- Exact 5 PHOTO + 5 VIDEO models (Flux (pulling thru XAI API), Grok Imagine (done through XAI API), seedance-2.0-turbo, wan-2.7, sora-2, veo-3.1)
- Real XAI + WaveSpeed routing with getWaveSpeedPath, /api/v3/ , polling, ref forwarding, owner bypass for kyle/scott/steve@shotbreak.io
- Self contained, sims + explicit DONE signals

See updated DO_THIS_NOW.txt in repo for the ritual, keys (XAI + WAVESPEED), and test steps.

Netlify site: capable-froyo-218eed (unpause if needed, set keys, clear cache + deploy, hard refresh).

"GET THE FUCK AFTER IT" executed: full golden now in local git ready to land on the provided GIT URL.