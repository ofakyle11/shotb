{
 "app.html::(op?180:0)": "Ternary between the two numeric literals 180 and 0 (CSS rotate degrees for the scene-header caret). No value flows in. Also cannot be esc()-wrapped: esc() maps falsy to '' so esc(0) would emit rotate(deg).",
 "app.html::done": "Counter the code computed: `let done = 0` in mhDoFinalGenerate (app.html:7565), incremented on each successful shot. Can be 0 at line 7621, so esc() would blank the 'N of M shots OK' number.",
 "app.html::errors.map(e => esc(e)).join('<br>')": "Each element is already wrapped in esc() by the map callback, and the '<br>' separator is deliberate markup. Escaping the joined result would double-escape the messages and print literal <br>.",
 "app.html::ex(t)": "ex() (app.html:2358) already escapes all five of & < > \" ' — verified character by character. The scanner just does not know the name. Output is XML written into a .docx zip via JSZip, never into the DOM.",
 "app.html::i": "Array index in every case: csLocked.forEach((c,i)=>) at 3309; p.scenes.forEach((s,i)=>) at 4771 and the same index passed to mhBgRenderMasterPanel(p,i) at 4810; p.characterBible.forEach((c,i)=>) passed to mhCharRenderCard(p,i) at 5527-5546; and mhCharRefreshDescOnly(i) at 5715, only ever called with that index. 0-valued, so esc() would blank it.",
 "app.html::i + 1": "Loop counter arithmetic: `for (let i = 0; i < keys.length; i++)` in mhDoFinalGenerate (app.html:7569), rendered as the human-readable shot number.",
 "app.html::j": "Numeric loop counter: `for(let j=0;j<p.scenes.length;j++)` at 4822 and 4890, and p.characterBible.forEach((other,j)=>) at 5558. Used as an <option value>. 0-valued, so esc() would blank it.",
 "app.html::k": "Shot index from `for(let k=0;k<shots.length;k++)` (app.html:4790) passed straight into mhBgRenderClipCard(p,i,k); rendered into data-k at 4876. 0-valued, so esc() would blank it.",
 "app.html::label ? ' — ' + esc(label) : ''": "The only value in the expression is already wrapped in esc(); the rest is the string literal ' — ' and an empty-string branch.",
 "app.html::mhClipSourceBadge(j)": "Every return path of mhClipSourceBadge (app.html:6428-6444) is a string literal in this file or ''. It interpolates nothing at all, and it returns markup on purpose, so escaping it would print the badge as text.",
 "app.html::model": "Already escaped: `const model = j && j.model ? esc(j.model) : 'video'` at the top of mhGeneratingClipHtml (app.html:6389). Wrapping again would double-escape and show &amp;-mangled model names.",
 "app.html::reasonList.join(' · ')": "reasonList is built on the line immediately above as '<strong>'+esc(f)+'</strong>: '+esc(sugR[f]) — both interpolated values are already esc()'d and the <strong> tags are intentional markup. Escaping the join would print the tags as text.",
 "app.html::sbProviderBadge(imgMode)": "Deliberate HTML producer (app.html:1569-1573). It emits a <span> whose only caller-derived fields, p.label and p.tip, are already passed through esc(); p.color comes only from the module-level literal table SB_PROVIDER_LABELS (app.html:1557-1568) or the literal '#888'. Escaping it would print the badge markup as visible text.",
 "app.html::sceneCount": "Code-computed number: const sceneCount=(p.scenes||[]).length (app.html:4196). Can be 0, so esc() would blank it.",
 "app.html::scenes.reduce((a,s)=>a+(s.shots||[]).length,0)": "Number the code computed: a sum of .length values. Can be 0, so esc() would blank the shot count.",
 "app.html::shi + 1": "Loop index arithmetic: the `shi` of (sc.shots || []).forEach((sh, shi) => ...) in mhRefreshBaselineShotSelect (app.html:8668).",
 "app.html::si": "Scene loop index, all 18 sites. It is the `si` of rD.scenes.forEach((sc,si)=>) (2345,2349,2350) and p.scenes.forEach((s,si)=>) (5979-5990), or that same numeric index threaded as a parameter into mhBgRenderClipCard(p,si,k) (4856) and its children mhBgRenderTextField/mhBgRenderCineField (4949-4989), whose only callers pass the loop index. It is 0-valued, so esc() would blank it (esc maps falsy to '').",
 "app.html::si + 1": "Loop index arithmetic: the `si` of scenes.forEach((sc, si) => ...) in mhRefreshBaselineShotSelect (app.html:8667).",
 "app.html::totalClips": "Number the code itself computed: Object.values(byScene).reduce((s,a)=>s+a.length,0). It can be 0 at this line (the !totalClips guard is on the next line), so esc() would blank the count.",
 "app.html::totalCst": "Counter the code computed in the same block as totalShots (`let totalCst=0`, incremented when sh.background.sources[k]==='cst'). Number, 0-valued.",
 "app.html::totalLocked": "Counter the code computed in the same block as totalShots (`let totalLocked=0`, incremented when sh.background.locked). Number, 0-valued.",
 "app.html::totalShots": "Counter the code computed: declared `let totalShots=0` and incremented by `totalShots++` in the scene/shot walk immediately above (app.html:4719-4728). Can be 0, so esc() would blank it.",
 "app.html::totalSug": "Counter the code computed in the same block as totalShots (`let totalSug=0`, incremented when sh.background.sources[k]==='sug'). Number, 0-valued.",
 "boards/boards.js::opts(S.ANGLES, sh.angle)": "Same local opts() at boards/boards.js:43: output is literals from this file plus esc(o) only. sh.angle is used solely for the `o === cur` selected-check and is never emitted into markup. Escaping the call would destroy the <option> tags opts returns.",
 "boards/boards.js::opts(S.MOVES, sh.move)": "Same local opts() at boards/boards.js:43: output is literals from this file plus esc(o) only. sh.move is used solely for the `o === cur` selected-check and is never emitted into markup. Escaping the call would destroy the <option> tags opts returns.",
 "boards/boards.js::opts(S.SIZES, sh.size)": "opts() is a local function defined at boards/boards.js:43. After my edit its body is `'<option' + (o === cur ? ' selected' : '') + '>' + esc(o) + '</option>'` — every fragment it emits is either a string literal in this same file or esc(o), so its return value cannot carry markup regardless of what is passed in. The second argument (sh.size, from localStorage SB_Boards_v1) is used ONLY in the `o === cur` comparison and is never interpolated into the output. Wrapping the call site in esc() would escape opts' own <option> tags and break the selects.",
 "casting/index.html::roleStatusTag(r)": "roleStatusTag is defined 11 lines above the sink in casting/index.html and deliberately returns markup, so escaping it would print literal <span> tags in the Status column. All three of its return paths are literal HTML built in that same function: (1) '<span class=\"co-tag booked\">booked · ' + esc(booked.name) + '</span>' — the only variable is already esc()'d; (2) '<span class=\"co-tag\">' + esc(offers) + ' in play</span>' — offers is Array.prototype.filter().length over r.candidates, and I additionally wrapped it in esc() as part of this pass so the claim holds even if that value is ever poisoned; (3) '<span class=\"co-tag\">open</span>' — a bare literal. No caller-supplied or localStorage-supplied string reaches the output unescaped.",
 "dashboard.html::escHtml(String(job.label).slice(0, 60))": "Already escaped. job.label comes from clip data in SB_Timeline_v1; it is String()-coerced, truncated, and then passed through escHtml() (dashboard.html:2144). Order is slice-then-escape, so the 60-char cut cannot split an entity. Flagged only because SAFE_CALL's /^esc\\b/ does not match 'escHtml'.",
 "dashboard.html::escHtml(String(job.status).toUpperCase())": "Already escaped. job.status comes from SB_Timeline_v1 clips, but it is String()-coerced and then passed through escHtml() (dashboard.html:2144, escapes all five). Escape happens after .toUpperCase(), so no entity is mangled. Flagged only because SAFE_CALL's /^esc\\b/ does not match 'escHtml'.",
 "dashboard.html::escHtml(e.cls)": "Already escaped by escHtml() (dashboard.html:2144) inside the double-quoted class attribute. The value is also only ever one of the literals 'cyan', 'violet', 'ok', 'magenta' pushed into `entries` at dashboard.html:2342-2358 in this same file. Scanner false positive on the escHtml name.",
 "dashboard.html::escHtml(e.tag)": "Already escaped by escHtml() (dashboard.html:2144). The value is also only ever one of the literals 'WRITER', 'BOARDS', 'STUDIO', 'BUDGET', 'CREW', 'PROPS', 'SETS', 'EDITOR' pushed into `entries` at dashboard.html:2342-2358 in this same file. Scanner false positive on the escHtml name.",
 "dashboard.html::escHtml(e.text)": "Already escaped. e.text is assembled from live store counts and is wrapped in escHtml() (dashboard.html:2144, escapes & < > \" '). Flagged only because the scanner's SAFE_CALL regex /^(esc|...)\\b/ cannot match the name 'escHtml'; double-wrapping would show literal entities in the activity feed.",
 "dashboard.html::escHtml(f.key)": "Already escaped by escHtml() (dashboard.html:2144) at all three sites (for=, id=, data-tier=), each of which is double-quoted. f.key is additionally a lookup into EST_TIER_FIELDS, a module-level constant table defined literally at dashboard.html:2385-2394 in this same file ('scale', 'director', 'lead', 'supporting', 'crew', 'locations', 'equipment', 'vfx'), so escaping is a no-op and the element id / data-tier name are byte-identical. Scanner false positive on the escHtml name.",
 "dashboard.html::escHtml(list[j].id)": "Already escaped by escHtml() (dashboard.html:2144) inside the double-quoted option value attribute. The source is window.SBBudget.TIERS, a static literal table in js/budget-engine.js:63 (ids are bare words such as 'micro', 'indie', 'studio') — no localStorage or archive data reaches it. Scanner false positive on the escHtml name.",
 "dashboard.html::escHtml(names[i])": "Already escaped by escHtml() (dashboard.html:2144). names is a local array literal declared one line earlier at dashboard.html:2539 — ['Above the line', 'Production (below the line)', 'Post-production', 'Other'] — and i is the loop index, so nothing external can reach it. Scanner false positive on the escHtml name.",
 "dashboard.html::escHtml(p.name)": "Already escaped. p.name is the vault slot name / project title (attacker-controlled) but it is wrapped in escHtml(), the file's own helper at dashboard.html:2144, which replaces all five of & < > \" ' . It is flagged only because the scanner's SAFE_CALL regex is /^(esc|...)\\b/ and \\b does not match between 'esc' and 'Html', so the name escHtml is unrecognised. Wrapping it a second time would render literal &amp;-entities in the title column.",
 "dashboard.html::escHtml(st.badge)": "Already escaped by escHtml() (dashboard.html:2144, escapes & < > \" '), inside the double-quoted class attribute. The value itself is also one of six string literals assigned in projectStats at dashboard.html:2210-2216 ('script', 'mastered', 'grading', 'generating', 'storyboard'). Scanner false positive: SAFE_CALL's /^esc\\b/ does not match 'escHtml'.",
 "dashboard.html::escHtml(st.pct)": "Already escaped by escHtml() (dashboard.html:2144) at both the style=\"width: ...%\" and the .prod-progress__pct text site. The value is also a number the code computed itself: pct is built by integer += in projectStats and returned as Math.min(100, pct) (dashboard.html:2202-2225). Scanner false positive on the escHtml name.",
 "editor/cut-ui.js::inner": "clipEl(cls, id, left, width, inner, track) at /home/user/shotb/editor/cut-ui.js:213 takes `inner` as an already-composed HTML fragment (it intentionally contains <b>, <span class=\"ed-clipmeta\"> and <canvas class=\"ed-wave\">), so escaping it would render the clip chrome as literal text and change what the UI displays. It has exactly three call sites, all in this same file, and after my edits every attacker-reachable value inside each of them is escaped: line 225-228 (esc(c.label || 'Clip'), C.effDur(c).toFixed(1) which is a number, esc(c.speed), esc(c.srcId)); line 231 (esc(ti.text.slice(0,22))); line 234-235 (esc(a.label.slice(0,20)), esc(a.srcId)). The only other content is literal markup written in this file. Verified by grepping every clipEl( call site.",
 "finance/index.html::fm(r.budget)": "fm is defined in this same file (finance/index.html:99) as `return '$' + Math.round(n).toLocaleString();`. Math.round always evaluates to a Number primitive (NaN at worst) no matter what object or string is passed, and Number.prototype.toLocaleString emits only digits, group/decimal separators and a sign — it can never emit & < > \" or '. A hostile r.budget from a shared archive is destroyed by the Math.round coercion before it reaches the string.",
 "finance/index.html::fm(r.committed)": "Same fm() helper (finance/index.html:99): '$' + Math.round(n).toLocaleString(). The Math.round coercion guarantees a Number primitive, so the result contains only digits, separators and a sign — no markup character can survive, whatever r.committed holds.",
 "finance/index.html::fm(r.efc)": "Same fm() helper (finance/index.html:99). Math.round(n) is a Number primitive for any input, and Number.prototype.toLocaleString cannot produce & < > \" or '. r.efc is a cost-report figure and cannot carry markup through fm.",
 "finance/index.html::fm(rep.totals.budget)": "Same fm() helper (finance/index.html:99). rep.totals.* are sums accumulated by M.costReport, and regardless of their input they pass through Math.round, which yields a Number primitive whose toLocaleString output is digits/separators/sign only.",
 "finance/index.html::fm(rep.totals.committed)": "Same fm() helper (finance/index.html:99): the Math.round coercion reduces any value to a Number primitive, so toLocaleString can only emit digits, separators and a sign — never a markup character.",
 "finance/index.html::fm(rep.totals.efc)": "Same fm() helper (finance/index.html:99). Math.round guarantees a Number primitive and Number.prototype.toLocaleString cannot emit & < > \" or ', so this interpolation cannot carry markup.",
 "js/budget-engine.js::escT(String(value))": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. Flagged only because the scanner's SAFE_CALL regex does not recognise the name 'escT'.",
 "js/budget-engine.js::escT(_aiNotes)": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. This is the agent-response text sink, so the escape matters; it is present and complete.",
 "js/budget-engine.js::escT(bm.genre)": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. Flagged only because the scanner's SAFE_CALL regex does not recognise the name 'escT'.",
 "js/budget-engine.js::escT(gname)": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. Flagged only because the scanner's SAFE_CALL regex does not recognise the name 'escT'.",
 "js/budget-engine.js::escT(k)": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. Flagged only because the scanner's SAFE_CALL regex does not recognise the name 'escT'.",
 "js/budget-engine.js::escT(labelTxt)": "Already HTML-escaped. escT() (js/budget-engine.js:789) is this file's escaper and, after my fix, escapes all five of & < > \" '. The scanner's SAFE_CALL pattern only matches names like esc/escAttr/escapeHtml, so the trailing 'T' makes an already-safe call read as unreviewed.",
 "js/budget-engine.js::escT(prod.tiers.vfx.label)": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. Flagged only because the scanner's SAFE_CALL regex does not recognise the name 'escT'.",
 "js/budget-engine.js::escT(rec.label)": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. Flagged only because the scanner's SAFE_CALL regex does not recognise the name 'escT'.",
 "js/budget-engine.js::escT(rec.note)": "Already HTML-escaped by escT() (js/budget-engine.js:789), which now escapes & < > \" and '. Flagged only because the scanner's SAFE_CALL regex does not recognise the name 'escT'.",
 "js/budget-engine.js::fmtMins(r.wallMinutes)": "fmtMins() is defined in this same file (js/budget-engine.js:783-788). Its first statement is min = Math.max(1, Math.round(min)); both return paths are numbers concatenated with the literals ' min', 'h ' and 'm'. No caller-supplied text can reach the output.",
 "js/budget-engine.js::fmtMoney(bm.med)": "Same in-file numeric formatter: fmtMoney() (js/budget-engine.js:775-781) rounds its argument first, and every return path is '$' plus a number or a .toFixed() string plus a literal suffix.",
 "js/budget-engine.js::fmtMoney(prod.total.high)": "Same in-file numeric formatter: fmtMoney() (js/budget-engine.js:775-781) rounds its argument first, and every return path is '$' plus a number or a .toFixed() string plus a literal suffix.",
 "js/budget-engine.js::fmtMoney(prod.total.likely)": "Same in-file numeric formatter: fmtMoney() (js/budget-engine.js:775-781) rounds its argument first, and every return path is '$' plus a number or a .toFixed() string plus a literal suffix.",
 "js/budget-engine.js::fmtMoney(prod.total.low)": "Same in-file numeric formatter: fmtMoney() (js/budget-engine.js:775-781) rounds its argument first, and every return path is '$' plus a number or a .toFixed() string plus a literal suffix.",
 "js/budget-engine.js::fmtMoney(r.highUsd)": "Same in-file numeric formatter: fmtMoney() (js/budget-engine.js:775-781) rounds its argument first, and every return path is '$' plus a number or a .toFixed() string plus a literal suffix.",
 "js/budget-engine.js::fmtMoney(r.likelyUsd)": "Same in-file numeric formatter: fmtMoney() (js/budget-engine.js:775-781) rounds its argument first, and every return path is '$' plus a number or a .toFixed() string plus a literal suffix.",
 "js/budget-engine.js::fmtMoney(r.onePassUsd)": "fmtMoney() is defined in this same file (js/budget-engine.js:775-781). Its first statement is n = Math.max(0, Math.round(n)), and every return path is '$' + a number or '$' + n.toFixed(...) + a literal 'B'/'M'/'k' suffix. No caller-supplied text can reach the output, whatever the argument is.",
 "js/budget-engine.js::fmtMoney(sel.likelyUsd)": "Same in-file numeric formatter: fmtMoney() (js/budget-engine.js:775-781) rounds its argument first, and every return path is '$' plus a number or a .toFixed() string plus a literal suffix.",
 "locations/index.html::apply": "apply is a local HTML fragment built four lines above (locations/index.html lines 285-287) from string literals in this same file; both branches are '<a class=\"tb-btn gold\" target=\"_blank\" rel=\"noopener\" href=\"' + esc(...) + '\">…</a>', so the only interpolated values (p.applyUrl and S.searchLink(...)) are already run through esc(). Escaping the fragment again would render the anchor tag as literal text instead of a link.",
 "locations/index.html::fee": "Not an HTML sink. Line 261 is $('skLocs').querySelector('input[data-fee=\"' + fee + '\"]') — a CSS attribute selector, never assigned to innerHTML; the matched element is only read for .value (line 262). fee is the decoded value of the data-feego attribute, and the matching data-fee attribute is written with esc() at line 196, so HTML-escaping fee here would make the selector stop matching. Verified by reading the click handler at lines 258-270.",
 "music/index.html::opts(M.SCOPES, c.scope)": "Same helper: opts() at music/index.html:142-146 escapes every value it emits with esc() (which covers all five of & < > \" '), and c.scope is only compared with === to decide the literal ' selected' — never interpolated. The return value is finished HTML, so wrapping it again would break the scope select.",
 "music/index.html::opts(M.STATUSES, c.status)": "Same helper: opts() at music/index.html:142-146 escapes every emitted value with esc(), and c.status is only used in a === comparison for the literal ' selected' — never printed. The return value is already-escaped markup; re-escaping would render literal <option> tags in the status select.",
 "music/index.html::opts(M.USES, c.use)": "opts() is defined at music/index.html:142-146 and returns already-escaped markup: it emits <option value=\"' + esc(v) + '\">' + esc(v) + '</option> for each v in the list, and its second argument (c.use) is only used in a `v === cur` comparison to append the literal ' selected' — it is never printed. Escaping the return value would double-encode the tags and render literal <option> text inside the select.",
 "post/index.html::(r.blockedBy.length ? r.blockedBy.map(function (b) { return esc(names[b] || b); }).join('": "Scanner artifact of the line-based concat matcher — the expression it clipped is a ternary whose only non-literal part is r.blockedBy.map(...), and every element is already passed through the file's esc() (line 118, escapes all five of & < > \" ') before .join(' + '). The alternate branch is the literal '—'. Double-wrapping would double-escape and change what the UI displays.",
 "post/index.html::s.path.map(function (id) { return esc(names[id] || id); }).join(' → ')": "Every array element is already wrapped in the file's complete esc() (line 118) before joining; the separator ' → ' and nothing else is unescaped. names[] is built from s.rows in this file and even a prototype-chain key (e.g. 'constructor') is stringified through esc(). Escaping again would render &amp; etc. literally.",
 "producer/budget-sheet.js::fm(cf.prep)": "Same wrapper: fm(n) = SBBudget.fmtMoney(n) (js/budget-engine.js:775-781), which rounds its argument and returns '$' plus a number/.toFixed() string plus a literal suffix on every path.",
 "producer/budget-sheet.js::fm(itemEst(it))": "Same wrapper: fm(n) = SBBudget.fmtMoney(n) (js/budget-engine.js:775-781). itemEst() (producer/budget-sheet.js:59-63) also returns only num()-derived arithmetic, and num() returns isFinite(n) ? n : 0 — a number in every case.",
 "producer/budget-sheet.js::fm(repM.totals.efc)": "Same wrapper: fm(n) = SBBudget.fmtMoney(n) (js/budget-engine.js:775-781). Even though repM comes from CMoney.costReport in another module, fmtMoney rounds the value first, so every return path is '$' plus a number and literals.",
 "producer/budget-sheet.js::fm(tot.subtotal)": "fm(n) (producer/budget-sheet.js:218) is a one-line wrapper for SBBudget.fmtMoney, which I read at js/budget-engine.js:775-781: it does n = Math.max(0, Math.round(n)) and every return path is '$' + a number or a .toFixed() string + a literal suffix, so the return value cannot contain markup no matter what is passed in.",
 "producer/incentives.js::fm(r.net)": "fm(n) (producer/incentives.js:15) is a one-line wrapper for SBBudget.fmtMoney (js/budget-engine.js:775-781), which rounds its argument and returns '$' plus a number or .toFixed() string plus a literal suffix on every path. r.net is also plain arithmetic (budget - recMid, producer/incentives.js:27).",
 "producer/index.html::name": "Not an HTML sink. The value is interpolated into a CSS selector passed to document.querySelector('.ps-tab[data-tab=\"'+name+'\"]') at producer/index.html:190 and never reaches innerHTML; a hostile value would throw a selector SyntaxError, not execute. Both call sites pass one of four literal tab names: b.dataset.tab from the static .ps-tab buttons (line 201) and location.hash only after an indexOf(['budget','schedule','incentives','sales']) whitelist check (lines 207, 210). HTML-escaping it would corrupt the selector without adding protection.",
 "producer/sales-forecast.js::r[1](w)": "r is an element of the literal array declared inline at sales-forecast.js:263-273; r[1] is one of exactly nine formatter functions written literally in that array, and I read every one. Each now returns only literals plus esc(fm(...)) output, and two of them ('= Lifetime revenue' and the NET row path) return intentional <b> markup, so escaping the call would double-escape and show literal tags to the user. No attacker-controlled value reaches this call unescaped.",
 "producer/schedule-board.js::(castRows || '<tr><td colspan=\"2\">—</td></tr>')": "castRows is the <tr> markup built at schedule-board.js:308-310; both of its values are escaped (esc(r.name), esc(r.codes[d])). The || fallback is a string literal on this line. Escaping the fragment would double-escape intentional table markup.",
 "producer/schedule-board.js::(locs.map(esc).join(' · ') || '—')": "Every element is passed through this file's esc() helper before the join (locs.map(esc)); the separator ' · ' and the '—' fallback are string literals on this line. It is already fully escaped — the scanner only misses it because the expression does not start with esc(. Re-escaping the joined result would turn &amp; into &amp;amp; in displayed location names, changing what the UI shows.",
 "producer/schedule-board.js::(sceneRows || '<tr><td colspan=\"5\">No scenes scheduled</td></tr>')": "sceneRows is the <tr> markup built at schedule-board.js:311-313; after my edits every value in it is escaped — esc(sc.num), esc(sc.heading), esc(formatEighths(sc.eighths)), esc(sc.cast.join(', ')), esc(sc.extras) — and the D/N cell picks between the literals 'N' and 'D'. The || fallback is a string literal on this line. Escaping the fragment would double-escape intentional table markup and destroy the call sheet.",
 "producer/schedule-board.js::k": "The one remaining hit (schedule-board.js:291) is wrap.querySelector('input[data-tag=\"' + k + '\"]') — a CSS selector passed to querySelector, not an HTML sink. k iterates TAG_KEYS, the module-level literal array ['stunts','sfx','vfx','water','animals','vehicles'] defined at line 39 of this same file, so it can never be attacker-controlled. HTML-escaping a selector would be the wrong encoding anyway. The genuine HTML use of the same k (line 257, data-tag attribute) is now wrapped in esc().",
 "producer/schedule-board.js::tagBoxes": "tagBoxes is an HTML fragment assembled 14 lines above (schedule-board.js:256-259) from string literals plus esc(k), a literal-picked ' checked', and k.charAt(0).toUpperCase()+k.slice(1) where k iterates TAG_KEYS — a module-level array of literal strings defined at line 39 of this same file. It is intentional <label>/<input> markup; escaping it would double-escape the checkboxes and break the breakdown editor. Every attacker-reachable value inside it is already escaped.",
 "taxcredit/index.html::deltaCls": "Assigned one line above the sink (line 186): \"var deltaCls = model.delta < 0 ? 'tc-neg' : 'tc-pos';\". Its only two possible values are class-name literals written in this file; no external data reaches it. It is already inside a double-quoted class attribute.",
 "taxcredit/index.html::fm(model.advisorModel)": "Same fm() numeric formatter defined at line 105 of this file — Math.round() forces a Number primitive and toLocaleString() cannot emit markup characters. model.advisorModel is Math.round(bt * qp * mid) in lib-taxcred.js line 120.",
 "taxcredit/index.html::fm(model.budgetCap)": "Same fm() numeric formatter defined at line 105 of this file. Even though model.budgetCap originates from the JURIS table, fm() runs it through Math.round() first, which yields a Number primitive (or NaN) in every case, so toLocaleString() can only produce numeric text.",
 "taxcredit/index.html::fm(model.estCredit)": "Same fm() numeric formatter defined at line 105 of this file — sole return path is '$' + Math.round(n).toLocaleString(), which cannot emit < > & \" '. model.estCredit is itself computed as 0 or Math.round(rawCredit) in lib-taxcred.js line 119.",
 "taxcredit/index.html::fm(model.qualifiedSpend)": "Same fm() numeric formatter defined at line 105 of this file; its single return path is '$' + Math.round(n).toLocaleString(), which cannot produce markup characters for any input. The value itself is also Math.round()ed in T.creditModel (lib-taxcred.js line 124) before it ever reaches here.",
 "taxcredit/index.html::fm(model.totalSpend)": "Same fm() numeric formatter defined at line 105 of this file — '$' + Math.round(n).toLocaleString() cannot carry markup for any input. model.totalSpend is Math.round(totalSpend) in lib-taxcred.js line 124.",
 "taxcredit/index.html::fm(r.amount)": "fm() is defined in this same file (line 105) as \"'$' + Math.round(n).toLocaleString()\". Math.round always returns a Number primitive regardless of input, and Number.prototype.toLocaleString can only emit digits, group/decimal separators, a sign, 'NaN' or '∞' — never < > & \" '. Verified empirically against markup strings, objects, arrays, null and Infinity: output was always $-prefixed numeric text.",
 "timeline/timeline-budget.js::escT(String(value))": "Pre-existing escT() call in statChip(); escT (line 860) now escapes all five of & < > \" ', so no markup can survive.",
 "timeline/timeline-budget.js::escT(_aiNotes)": "Pre-existing escT() call wrapping the line-producer text returned by the crew agent. escT now escapes all five characters, so this remote-sourced prose is rendered as text only.",
 "timeline/timeline-budget.js::escT(ai.clipCount)": "I wrapped this in escT(). escT (line 860) escapes & < > \" ' and handles 0 correctly (it tests s == null, not falsiness), so the digit is still rendered.",
 "timeline/timeline-budget.js::escT(ai.concurrency)": "I wrapped both occurrences (line 995 inside value=\"...\" of #budConc, line 1007 in text). Source is the localStorage prefs blob; escT escapes all five characters.",
 "timeline/timeline-budget.js::escT(ai.retakeFactor)": "I wrapped this in escT(); it lands in value=\"...\" of #budRetake and is re-read from the SB_* prefs blob in localStorage via prefs().retakeFactor. escT escapes both quote characters, so the attribute cannot be closed.",
 "timeline/timeline-budget.js::escT(bm.genre)": "Pre-existing escT() call; escT (line 860) escapes & < > \" ' so the genre string cannot introduce markup.",
 "timeline/timeline-budget.js::escT(bm.percentile)": "I wrapped this in escT(). budgetPercentile() (lines 327-337) returns Math.round(clamp(...)) so it is numeric, but escaping a number costs nothing and escT preserves 0 correctly.",
 "timeline/timeline-budget.js::escT(d.count)": "I wrapped this (lines 962 and 979) in escT(). Driver objects on line 962 originate in root.SBDoc.analyzeDoc(), outside this file, so it is not treated as trusted; escT's five-character escaping neutralises it.",
 "timeline/timeline-budget.js::escT(d.weight)": "I wrapped this (both occurrences, lines 962 and 979) in escT(); it sits inside title=\"weight ...\" and drivers on line 962 come from root.SBDoc.analyzeDoc(), an external module. escT escapes all five characters so the quoted attribute cannot be broken.",
 "timeline/timeline-budget.js::escT(ds.footageHours)": "I wrapped this in escT(); like the rest of the ds.* group it is produced by root.SBDoc.estimate() in another module, so escaping is the only guarantee available to this file.",
 "timeline/timeline-budget.js::escT(ds.shootDays)": "I wrapped this in escT(). prod.docSchedule comes from root.SBDoc.estimate() in timeline-doc.js, a file I do not own and cannot vouch for, so it is escaped rather than trusted.",
 "timeline/timeline-budget.js::escT(g.label)": "Pre-existing escT() call on a grant-program label sourced from root.SBDoc; escT's five-character escaping neutralises it regardless of what that module returns.",
 "timeline/timeline-budget.js::escT(g.note || '')": "Pre-existing escT() call on a grant note from root.SBDoc; escT (line 860) escapes & < > \" ' so the <td> content cannot carry markup.",
 "timeline/timeline-budget.js::escT(gname)": "Pre-existing escT() call on a department-group key; escT's five-character escaping means the <th> content cannot carry markup.",
 "timeline/timeline-budget.js::escT(k)": "Pre-existing escT() call on a budget line-item key; escT (line 860) escapes & < > \" ' so the <td> content is inert.",
 "timeline/timeline-budget.js::escT(labelTxt)": "Pre-existing escT() call; escT (line 860) now escapes & < > \" ' so its output cannot break out of the surrounding <span> or any quoted attribute.",
 "timeline/timeline-budget.js::escT(prod.recovery.label)": "Pre-existing escT() call; escT (line 860) now escapes all five characters, so an incentive label from the INCENTIVES table cannot break out of the surrounding <span>.",
 "timeline/timeline-budget.js::escT(prod.schedule.shootDays)": "I wrapped this in escT(). In documentary mode prod.schedule is copied from root.SBDoc's estimate, so it is not a value this file computes end to end.",
 "timeline/timeline-budget.js::escT(prod.tiers.vfx.label)": "Pre-existing escT() call; escT (line 860) now escapes all five of & < > \" ', so the label cannot introduce markup.",
 "timeline/timeline-budget.js::escT(rec.label)": "Pre-existing escT() call; output of escT cannot contain & < > \" ' after the helper fix.",
 "timeline/timeline-budget.js::escT(rec.note)": "Pre-existing escT() call. The note is joined from incentive.note and root.SBDoc.DOC_INCENTIVE_ADJUST notes (external), but escT's five-character escaping makes the joined string inert.",
 "timeline/timeline-budget.js::escT(t.id)": "Output of escT(), this file's own HTML escaper (line 860), which after my fix replaces all five of & < > \" ' — the result cannot contain a markup-significant character. Value itself is untrusted (tier ids reach here from root.SBDoc.DOC_TIERS, an external module), which is why I wrapped it.",
 "timeline/timeline-budget.js::fmtMins(r.wallMinutes)": "fmtMins() is defined at lines 854-859 of this file; it starts with min = Math.max(1, Math.round(min)) and every return path is that number concatenated with the literals ' min', 'h ' and 'm'. Non-numeric input yields 'NaNh '. It cannot emit markup.",
 "timeline/timeline-budget.js::fmtMoney(bm.med)": "fmtMoney() (lines 846-853) reduces its argument to a number with Math.max(0, Math.round(n)) before building '$' + digits, so its output is markup-free on every path.",
 "timeline/timeline-budget.js::fmtMoney(prod.total.high)": "Same fmtMoney() at lines 846-853: numeric coercion up front, output is always '$' plus a digit string. It cannot emit a markup character.",
 "timeline/timeline-budget.js::fmtMoney(prod.total.likely)": "Same fmtMoney() at lines 846-853: the argument is numerically coerced before any string is built, so the result is always '$' plus digits.",
 "timeline/timeline-budget.js::fmtMoney(prod.total.low)": "fmtMoney() (lines 846-853) coerces its argument with Math.max(0, Math.round(n)) and returns '$' plus digits or '$NaN' on every path, so no markup can reach the DOM.",
 "timeline/timeline-budget.js::fmtMoney(r.highUsd)": "Same fmtMoney() at lines 846-853: output is always '$' plus a digit string produced by Math.round/.toFixed, or '$NaN'. Markup cannot pass through.",
 "timeline/timeline-budget.js::fmtMoney(r.likelyUsd)": "Same fmtMoney() at lines 846-853: n is coerced by Math.max(0, Math.round(n)) before use, so every return is '$' plus digits (or '$NaN'). It cannot emit < > \" ' or &.",
 "timeline/timeline-budget.js::fmtMoney(r.onePassUsd)": "fmtMoney() is defined at lines 846-853 of this file and every return path is '$' concatenated with a value that has already passed through Math.max(0, Math.round(n)) and at most .toFixed()/.replace() on digits. A non-numeric input becomes NaN, yielding the literal '$NaN'. No caller-supplied character can reach the output.",
 "timeline/timeline-budget.js::fmtMoney(sel.likelyUsd)": "Same fmtMoney() at lines 846-853: every return path is '$' plus a Math.round/.toFixed digit string, or '$NaN'. No markup character can pass through.",
 "timeline/timeline-characters.js::label": "label is the first parameter of field() (line 427). field() has exactly six call sites, lines 411-416 of this same file, and every one passes a string literal ('Description', 'Body type', 'Wardrobe', 'Voice profile', 'Default emotion', 'Cast role'). No caller-supplied data can reach it.",
 "timeline/timeline-characters.js::sel": "sel is assigned six lines above the sink, at line 393: `const sel = selected === n ? ' selected' : '';`. Both branches are string literals in this file, so the value interpolated into class=\"char-card...\" is always ' selected' or ''.",
 "timeline/timeline-characters.js::thumb": "thumb is deliberate markup built at line 390 in the same function: `c.refUrl ? '<img src=\"' + esc(c.refUrl) + '\" alt=\"\">' : '<span class=\"ph\">👤</span>'`. Its only variable part, c.refUrl, is already inside a double-quoted src and passed through esc(), which I fixed to escape both quote characters. Escaping thumb itself would render the tags as visible text and break the card layout.",
 "timeline/timeline-locations.js::sel": "sel is assigned at line 211, six lines above the sink: `const sel = selectedKey === loc.key ? ' selected' : '';`. Both branches are string literals in this file, so only ' selected' or '' reaches class=\"loc-card...\".",
 "timeline/timeline-locations.js::thumb": "thumb is deliberate markup built at lines 212-214 of the same function: `loc.plateUrl ? '<img src=\"' + esc(loc.plateUrl) + '\" alt=\"\">' : '<span class=\"ph\">📍</span>'`. The only variable part is inside a double-quoted src and already escaped by esc(), which I fixed to cover both quote characters. Escaping thumb would print the tags as literal text.",
 "timeline/timeline.js::label": "label is the third parameter of mkTog() (line 1187). mkTog() has exactly twelve call sites, all on lines 1178-1180 of this same file, and every one passes a string literal ('Location', 'Time', 'Weather', 'Season', 'Angle', 'Film grade', 'Color', 'Saturation', 'Lighting', 'Mood', 'FX', 'Sound').",
 "today/index.html::castNames.map(esc).join(' · ')": "Every element is already passed through this file's own esc() (today/index.html line 62, which escapes all five of & < > \" '); .map(esc) applies it per name and the ' · ' separator is a literal in this file. The scanner only fails to see it because the escaping call is not the head of the expression. Wrapping the joined string again would double-escape (a name containing & would render as &amp;), which the brief forbids.",
 "today/index.html::locs.map(esc).join('<br>')": "Same shape: each location name is escaped element-wise by this file's esc(), and the only markup in the result is the literal '<br>' separator written in this file, not attacker text. It cannot be wrapped in esc() as a whole because that would escape the intended <br> and print it as visible text.",
 "tools/lib-media.js::xmlEsc(e.path)": "Already wrapped by xmlEsc (now all five); unrecognised name only. Verified with a hostile path containing \" > < script tags and an apostrophe — fully entity-encoded on write and byte-identical after parseManifest/xmlUnesc.",
 "tools/lib-media.js::xmlEsc(e.sha256)": "Wrapped by xmlEsc (now all five). Flagged only because the scanner does not know that helper name. Hex digests are unaffected by escaping, and parseManifest's [0-9a-f]+ match still succeeds.",
 "tools/lib-media.js::xmlEsc(e.size || 0)": "Wrapped by xmlEsc (now all five). Flagged only because the scanner does not know that helper name. e.size can be any value from a shared archive, so it is escaped rather than trusted as a number.",
 "tools/lib-media.js::xmlEsc(meta.created || new Date().toISOString())": "Wrapped by this file's xmlEsc, which I fixed to escape all five of & < > \" '. The scanner's SAFE_CALL regex only knows the names esc/escAttr/escape/escapeHtml/htmlEsc/encodeURIComponent/csvSafe, so it does not recognise xmlEsc — the value itself is escaped.",
 "tools/lib-media.js::xmlEsc(meta.project)": "Already wrapped by xmlEsc (now all five of & < > \" '); flagged only because the scanner's SAFE_CALL list does not contain the name xmlEsc. Verified: a project name of O'Brien & <Co> emits O&#39;Brien &amp; &lt;Co&gt;.",
 "tools/sched-weather.js::(w ? esc(S.wmoLabel(w.code))": "Scanner truncates the expression at the first '+'; the real cell is (w ? esc(S.wmoLabel(w.code)) + ' · ' + esc(w.tmin) + '–' + esc(w.tmax) + '° · ' + esc(w.precipProb || 0) + '% rain' : '<span style=\"color:var(--dim)\">beyond forecast</span>'). Every interpolated value is now wrapped in this file's new esc(); the else branch is a string literal in this file. Nothing unescaped remains in it.",
 "tools/tools-core.js::s.summary(this.rows)": "Deliberate HTML producer. The register engine renders each section's own summary(), and those functions emit markup on purpose (<b> counts, <span class=\"tk-chip bad\"> warnings), so escaping here would print tags as text. Verified against all 17 summary providers in the repo — not 13, as an earlier note claimed: every value any of them interpolates is either a code-computed count (rows.filter(...).length, an incremented tally) or already wrapped in esc()/escAttr() inside the provider. Re-check this entry if a provider is ever added that interpolates a row field directly.",
 "tools/tools-media-ui.js::opts": "`opts` is not a value, it is an HTML fragment built 6 lines above in the same function: Object.keys(MD.SENSORS).map(...) joining '<option value=\"' + escAttr(k) + '\">' + esc(MD.SENSORS[k].label) + '</option>'. Both interpolations inside it are escaped (I added the escAttr on k in this pass), so no attacker text reaches markup unescaped. Escaping `opts` itself would encode the <option> tags and render them as literal text, destroying the sensor <select>.",
 "tools/tools-media-ui.js::v.missing.map(esc).join(', ') : '')": "Every element is already escaped individually by .map(esc) before the join; the scanner's regex swallowed the trailing ternary (` : '')`) into the captured expression, which is why it does not match the safe-call prefix. The joined string lands in text content inside <div class=\"tk-result\">, never in an attribute, so esc's coverage of & < > \" is sufficient there (the missing apostrophe only matters in a single-quoted attribute). Wrapping the whole expression again would double-encode, turning a path containing & into &amp;amp;. Values are manifest paths from MD.verifyAgainst -> parseManifest -> xmlUnesc, so they are attacker-controllable and do need the per-element esc that is already present.",
 "vfx/index.html::verdict": "`verdict` is a local var declared 3 lines above the sink (vfx/index.html:217-220) and assigned only from a 4-branch ternary whose every branch is a literal HTML string written in this file (`<span class=\"vx-tag\">no bid yet</span>`, `<span class=\"vx-under\">within range</span>`, and two `<span class=\"vx-over\">/<span class=\"vx-under\">` branches). It is intentionally markup, so escaping it would render visible tags. Its only interpolations are money(c.delta) and money(-c.delta), which I wrapped in esc() as part of this pass; money() is defined at vfx/index.html:121 as `'$' + (+n || 0).toLocaleString()`, i.e. a coerced number, never attacker text. No branch reads localStorage or shot fields.",
 "wardrobe/index.html::pieceRows(l)": "`pieceRows` is defined in this same file at wardrobe/index.html:181-189 and returns HTML by design (the <tr> rows of the piece table), so escaping it would render visible tags. After this pass every value it interpolates is escaped: esc(i) for the loop index in all four data-i/data-delpiece attributes, esc(p.item) for the item name, esc(p.cost || 0) for the cost, and esc(s) for the <option> text drawn from W.SOURCES. Everything else in its return value is a string literal written in this file, so it cannot carry attacker markup out to the caller.",
 "workflow/advisor-ui.js::s[0]": "s is `SEV[x.sev] || SEV.low` (line 58). SEV is a module-level constant defined literally at advisor-ui.js:21 with four entries whose values are literal arrays of the form ['#RRGGBB','LABEL']; nothing in the file ever writes to SEV. So s[0] is one of four literal hex colours — and if x.sev happens to name an inherited Object.prototype member, s[0] is undefined and stringifies to \"undefined\". Neither can carry markup into the style attribute. (x.sev itself is only used as a lookup key and is never interpolated.)",
 "workflow/workflow.js::c.status": "c iterates a.clips, which assess() builds in this same file (lines 176-181). Each element's status is `c.status === 'approved' ? 'approved' : c.status === 'generating' ? 'generating' : (c.videoUrl ? 'rendered' : 'queued')` — the stored clip status is only compared, never passed through, so the rendered value is always one of four in-file literals.",
 "workflow/workflow.js::k": "k is the loop variable of `['generating', 'queued', 'rendered', 'approved'].map(function (k) {...})` — an array literal written inline at workflow.js:258. It can only ever be one of those four literal strings from this file.",
 "workflow/workflow.js::st.status": "st comes from assess() in this same file, which render() calls directly (line 222). Every stage's status is written from a string literal in this file: the seven `status:` properties (lines 42, 59, 79, 100, 117, 135, 156) are ternaries between 'done', 'active' and 'todo', and line 166 assigns 'active'. A clip's or store's own stored status is only ever compared with ===, never copied into st.status, so this value is always one of three in-file literals. Covers both flagged lines, 233 and 238."
}
