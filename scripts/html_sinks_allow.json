{
 "app.html::(p.locationBible||[]).map(l=>'<option value=\"'+esc(l.key)+'\"'+(ph.locationKey===l.key?' selected':'')+'>'+esc(l.name)+'</option>').join('')": {
  "n": 1,
  "why": "Every value inside the mapped template is already wrapped: esc(l.key) in the option value and esc(l.name) in its text. The only unescaped part is the literal ' selected'."
 },
 "app.html::action": {
  "n": 1,
  "why": "Already escaped where it is assigned: `const action = esc(...)` at app.html:8763. The scanner sees the bare identifier at the sink and cannot see the escaping at the declaration."
 },
 "app.html::done": {
  "n": 2,
  "why": "SAFE, a counter. `done` is incremented in the batch loop and printed as 'Batch finished: N of M shots OK'. Code-computed integer, never user text."
 },
 "app.html::errors.map(e => esc(e)).join('<br>')": {
  "n": 1,
  "why": "Each element is already wrapped in esc() by the map callback, and the '<br>' separator is deliberate markup. Escaping the joined result would double-escape the messages and print literal <br>."
 },
 "app.html::i": {
  "n": 3,
  "why": "Array index in every case: csLocked.forEach((c,i)=>) at 3309; p.scenes.forEach((s,i)=>) at 4771 and the same index passed to mhBgRenderMasterPanel(p,i) at 4810; p.characterBible.forEach((c,i)=>) passed to mhCharRenderCard(p,i) at 5527-5546; and mhCharRefreshDescOnly(i) at 5715, only ever called with that index. 0-valued, so esc() would blank it."
 },
 "app.html::i + 1": {
  "n": 1,
  "why": "Loop counter arithmetic: `for (let i = 0; i < keys.length; i++)` in mhDoFinalGenerate (app.html:7569), rendered as the human-readable shot number."
 },
 "app.html::key": {
  "n": 25,
  "why": "SAFE, and this is the one worth reading. `key` is built at app.html:5375 as `${si}-${shi}` from two forEach indices, so it is always of the form \"0-1\" — digits and a hyphen, code-generated, never user text. It appears in id attributes, a data-shot-key attribute, and inside onclick JS strings such as toggleShotMinimize('${key}', ...). The onclick sites are the reason this entry is written out rather than waved through: a JS string inside an HTML attribute is the context where an unescaped value would be worst, and it is safe here ONLY because the value is synthesised from loop counters. If key ever derives from a shot name, a slug or anything an operator types, these become live XSS and this entry must go."
 },
 "app.html::label ? ' — ' + esc(label) : ''": {
  "n": 1,
  "why": "SAFE, already escaped. The only interpolated value is esc(label); the surrounding ternary picks between that and an empty string."
 },
 "app.html::lockTxt": {
  "n": 1,
  "why": "SAFE, a ternary over two literals. app.html:5409 — `ui.minimized ? 'UNLOCK' : 'LOCK'`. No external input reaches it."
 },
 "app.html::mhCharPhotosDomId(key)": {
  "n": 1,
  "why": "Returns 'char-photos-' plus the argument with everything outside [A-Za-z0-9_-] replaced by underscore (app.html:3457), so the result cannot carry a quote or an angle bracket."
 },
 "app.html::mhClipMediaHtml(j)": {
  "n": 1,
  "why": "Returns markup this file builds, with the URL through CinUrl.safe and every other value through esc(). Read at app.html:6477."
 },
 "app.html::mhClipSourceBadge(j)": {
  "n": 1,
  "why": "Every return path of mhClipSourceBadge (app.html:6428-6444) is a string literal in this file or ''. It interpolates nothing at all, and it returns markup on purpose, so escaping it would print the badge as text."
 },
 "app.html::minCls": {
  "n": 1,
  "why": "A ternary between the two string literals ' minimized' and ''. No value flows in."
 },
 "app.html::model": {
  "n": 1,
  "why": "Already escaped: `const model = j && j.model ? esc(j.model) : 'video'` at the top of mhGeneratingClipHtml (app.html:6389). Wrapping again would double-escape and show &amp;-mangled model names."
 },
 "app.html::o": {
  "n": 5,
  "why": "SAFE, a constant table. `o` iterates CINE.temp.o.slice(0,5) — a fixed list of colour-grade preset names defined in source. No input path."
 },
 "app.html::pi": {
  "n": 1,
  "why": "The array index from a forEach callback — a number."
 },
 "app.html::prov": {
  "n": 1,
  "why": "sbProviderBadge(...) output (app.html:1584). It escapes the tip and the label, and the only unescaped part is a colour taken from the SB_PROVIDER_LABELS constant or the literal '#888' fallback."
 },
 "app.html::renderShotChars(key, p)": {
  "n": 1,
  "why": "DELIBERATE raw HTML. renderShotChars returns markup for the character photo row and is trusted to escape its own values; interpolating its return is the intent. A trust decision, like js/ui-table.js::s.summary."
 },
 "app.html::sbProviderBadge(imgMode)": {
  "n": 1,
  "why": "Deliberate HTML producer (app.html:1569-1573). It emits a <span> whose only caller-derived fields, p.label and p.tip, are already passed through esc(); p.color comes only from the module-level literal table SB_PROVIDER_LABELS (app.html:1557-1568) or the literal '#888'. Escaping it would print the badge markup as visible text."
 },
 "app.html::scenes.reduce((a,s)=>a+(s.shots||[]).length,0)": {
  "n": 1,
  "why": "Number the code computed: a sum of .length values. Can be 0, so esc() would blank the shot count."
 },
 "app.html::shi + 1": {
  "n": 2,
  "why": "SAFE, arithmetic on a forEach index. `shi` is the shot index from shots.forEach((shot, shi) =>) at app.html:5374. Integer."
 },
 "app.html::shotLabel": {
  "n": 1,
  "why": "`S${si+1}.${shi+1}` from the same loop indices — digits and a dot. Rendered as text."
 },
 "app.html::si + 1": {
  "n": 2,
  "why": "SAFE, arithmetic on a forEach index. `si` is the scene index from scenes.forEach((scene, si) =>) at app.html:5372 — an integer, +1 for display. Cannot carry markup."
 },
 "app.html::src": {
  "n": 3,
  "why": "`const src = CinUrl.safe(j.videoUrl)` at app.html:6485. Used in img/video src and an href, which are URL contexts, so the scheme allow-list is the right check and HTML escaping would corrupt the query string."
 },
 "boards/boards.js::opts(S.ANGLES, sh.angle)": {
  "n": 1,
  "why": "Same local opts() at boards/boards.js:43: output is literals from this file plus esc(o) only. sh.angle is used solely for the `o === cur` selected-check and is never emitted into markup. Escaping the call would destroy the <option> tags opts returns."
 },
 "boards/boards.js::opts(S.MOVES, sh.move)": {
  "n": 1,
  "why": "Same local opts() at boards/boards.js:43: output is literals from this file plus esc(o) only. sh.move is used solely for the `o === cur` selected-check and is never emitted into markup. Escaping the call would destroy the <option> tags opts returns."
 },
 "boards/boards.js::opts(S.SIZES, sh.size)": {
  "n": 1,
  "why": "opts() is a local function defined at boards/boards.js:43. After my edit its body is `'<option' + (o === cur ? ' selected' : '') + '>' + esc(o) + '</option>'` — every fragment it emits is either a string literal in this same file or esc(o), so its return value cannot carry markup regardless of what is passed in. The second argument (sh.size, from localStorage SB_Boards_v1) is used ONLY in the `o === cur` comparison and is never interpolated into the output. Wrapping the call site in esc() would escape opts' own <option> tags and break the selects."
 },
 "casting/index.html::roleStatusTag(r)": {
  "n": 1,
  "why": "roleStatusTag is defined 11 lines above the sink in casting/index.html and deliberately returns markup, so escaping it would print literal <span> tags in the Status column. All three of its return paths are literal HTML built in that same function: (1) '<span class=\"co-tag booked\">booked · ' + esc(booked.name) + '</span>' — the only variable is already esc()'d; (2) '<span class=\"co-tag\">' + esc(offers) + ' in play</span>' — offers is Array.prototype.filter().length over r.candidates, and I additionally wrapped it in esc() as part of this pass so the claim holds even if that value is ever poisoned; (3) '<span class=\"co-tag\">open</span>' — a bare literal. No caller-supplied or localStorage-supplied string reaches the output unescaped."
 },
 "editor/cut-ui.js::inner": {
  "n": 1,
  "why": "clipEl(cls, id, left, width, inner, track) at /home/user/shotb/editor/cut-ui.js:213 takes `inner` as an already-composed HTML fragment (it intentionally contains <b>, <span class=\"ed-clipmeta\"> and <canvas class=\"ed-wave\">), so escaping it would render the clip chrome as literal text and change what the UI displays. It has exactly three call sites, all in this same file, and after my edits every attacker-reachable value inside each of them is escaped: line 225-228 (esc(c.label || 'Clip'), C.effDur(c).toFixed(1) which is a number, esc(c.speed), esc(c.srcId)); line 231 (esc(ti.text.slice(0,22))); line 234-235 (esc(a.label.slice(0,20)), esc(a.srcId)). The only other content is literal markup written in this file. Verified by grepping every clipEl( call site."
 },
 "festivals/index.html::feeCell(s.fee)": {
  "n": 1,
  "why": "Both branches escape: a parsed amount goes through esc(amt.toLocaleString()), and text that will not parse goes through esc(txt) inside a flagged span. It exists because the migration used to turn \"$1,200\" into 0 — now an unreadable fee shows the owner's own words and says so, rather than silently becoming a wrong number."
 },
 "finance/index.html::fm(r.budget)": {
  "n": 1,
  "why": "fm is defined in this same file (finance/index.html:99) as `return '$' + Math.round(n).toLocaleString();`. Math.round always evaluates to a Number primitive (NaN at worst) no matter what object or string is passed, and Number.prototype.toLocaleString emits only digits, group/decimal separators and a sign — it can never emit & < > \" or '. A hostile r.budget from a shared archive is destroyed by the Math.round coercion before it reaches the string."
 },
 "finance/index.html::fm(r.committed)": {
  "n": 1,
  "why": "Same fm() helper (finance/index.html:99): '$' + Math.round(n).toLocaleString(). The Math.round coercion guarantees a Number primitive, so the result contains only digits, separators and a sign — no markup character can survive, whatever r.committed holds."
 },
 "finance/index.html::fm(r.efc)": {
  "n": 1,
  "why": "Same fm() helper (finance/index.html:99). Math.round(n) is a Number primitive for any input, and Number.prototype.toLocaleString cannot produce & < > \" or '. r.efc is a cost-report figure and cannot carry markup through fm."
 },
 "finance/index.html::fm(rep.totals.budget)": {
  "n": 1,
  "why": "Same fm() helper (finance/index.html:99). rep.totals.* are sums accumulated by M.costReport, and regardless of their input they pass through Math.round, which yields a Number primitive whose toLocaleString output is digits/separators/sign only."
 },
 "finance/index.html::fm(rep.totals.committed)": {
  "n": 1,
  "why": "Same fm() helper (finance/index.html:99): the Math.round coercion reduces any value to a Number primitive, so toLocaleString can only emit digits, separators and a sign — never a markup character."
 },
 "finance/index.html::fm(rep.totals.efc)": {
  "n": 1,
  "why": "Same fm() helper (finance/index.html:99). Math.round guarantees a Number primitive and Number.prototype.toLocaleString cannot emit & < > \" or ', so this interpolation cannot carry markup."
 },
 "js/ui-table.js::chipCls(x.cls)": {
  "n": 1,
  "why": "chipCls (js/ui-table.js:99) strips everything outside [\\w\\s-], so a flag class lands as a class token and cannot end the attribute. test_ui_chrome asserts the emitted value matches /class=\"tk-chip [\\w\\s-]*\"/ and that no attribute named on* is created."
 },
 "js/ui-table.js::cols": {
  "n": 1,
  "why": "s.fields.length + (s.flags ? 2 : 1) — arithmetic on an array length, used as a colspan."
 },
 "js/ui-table.js::h": {
  "n": 1,
  "why": "An accumulator of markup this file just built: every chip is esc(x.label) inside a chipCls()-filtered class, and every cell is esc()'d where it is appended. Nothing enters h unescaped."
 },
 "js/ui-table.js::s.summary(this.rows)": {
  "n": 1,
  "why": "DELIBERATE raw HTML, and the one entry here that is a trust decision rather than an escape. `summary` is a function from the PAGE's own schema, not from any stored or fetched value — the same trust level as the page's inline script, and the Register this replaced behaved identically. A schema built from user input would be a defect in the caller; if that becomes possible, revisit this entry."
 },
 "locations/index.html::apply": {
  "n": 1,
  "why": "apply is a local HTML fragment built four lines above (locations/index.html lines 285-287) from string literals in this same file; both branches are '<a class=\"tb-btn gold\" target=\"_blank\" rel=\"noopener\" href=\"' + esc(...) + '\">…</a>', so the only interpolated values (p.applyUrl and S.searchLink(...)) are already run through esc(). Escaping the fragment again would render the anchor tag as literal text instead of a link."
 },
 "locations/index.html::fee": {
  "n": 1,
  "why": "Not an HTML sink. Line 261 is $('skLocs').querySelector('input[data-fee=\"' + fee + '\"]') — a CSS attribute selector, never assigned to innerHTML; the matched element is only read for .value (line 262). fee is the decoded value of the data-feego attribute, and the matching data-fee attribute is written with esc() at line 196, so HTML-escaping fee here would make the selector stop matching. Verified by reading the click handler at lines 258-270."
 },
 "music/index.html::opts(M.SCOPES, c.scope)": {
  "n": 1,
  "why": "Same helper: opts() at music/index.html:142-146 escapes every value it emits with esc() (which covers all five of & < > \" '), and c.scope is only compared with === to decide the literal ' selected' — never interpolated. The return value is finished HTML, so wrapping it again would break the scope select."
 },
 "music/index.html::opts(M.STATUSES, c.status)": {
  "n": 1,
  "why": "Same helper: opts() at music/index.html:142-146 escapes every emitted value with esc(), and c.status is only used in a === comparison for the literal ' selected' — never printed. The return value is already-escaped markup; re-escaping would render literal <option> tags in the status select."
 },
 "music/index.html::opts(M.USES, c.use)": {
  "n": 1,
  "why": "opts() is defined at music/index.html:142-146 and returns already-escaped markup: it emits <option value=\"' + esc(v) + '\">' + esc(v) + '</option> for each v in the list, and its second argument (c.use) is only used in a `v === cur` comparison to append the literal ' selected' — it is never printed. Escaping the return value would double-encode the tags and render literal <option> text inside the select."
 },
 "netlify/functions/auth.js::new Date().toLocaleString('en-US', { timeZone: 'America/New_York' })": {
  "n": 1,
  "why": "A formatted current date. No request value reaches it."
 },
 "post/index.html::(r.blockedBy.length ? r.blockedBy.map(function (b) { return esc(names[b] || b); }).join('": {
  "n": 1,
  "why": "Scanner artifact of the line-based concat matcher — the expression it clipped is a ternary whose only non-literal part is r.blockedBy.map(...), and every element is already passed through the file's esc() (line 118, escapes all five of & < > \" ') before .join(' + '). The alternate branch is the literal '—'. Double-wrapping would double-escape and change what the UI displays."
 },
 "post/index.html::act2": {
  "n": 1,
  "why": "One of two constant markup literals — the link to /distribution/ shown when a deliverable is recorded done, or the dim note shown when it is not. Nothing is interpolated into either branch."
 },
 "post/index.html::s.path.map(function (id) { return esc(names[id] || id); }).join(' → ')": {
  "n": 1,
  "why": "Every array element is already wrapped in the file's complete esc() (line 118) before joining; the separator ' → ' and nothing else is unescaped. names[] is built from s.rows in this file and even a prototype-chain key (e.g. 'constructor') is stringified through esc(). Escaping again would render &amp; etc. literally."
 },
 "post/index.html::slipCell(r)": {
  "n": 1,
  "why": "Returns constant markup only. The class is picked from the three literals 'late'/'early'/'', the slip text is built from the numeric r.slip and passed through esc(), and the forecast date is esc()'d. No caller value reaches the markup unescaped."
 },
 "producer/budget-sheet.js::fm(cf.prep)": {
  "n": 1,
  "why": "Same wrapper: fm(n) = SBBudget.fmtMoney(n) (js/budget-engine.js:775-781), which rounds its argument and returns '$' plus a number/.toFixed() string plus a literal suffix on every path."
 },
 "producer/budget-sheet.js::fm(itemEst(it))": {
  "n": 1,
  "why": "Same wrapper: fm(n) = SBBudget.fmtMoney(n) (js/budget-engine.js:775-781). itemEst() (producer/budget-sheet.js:59-63) also returns only num()-derived arithmetic, and num() returns isFinite(n) ? n : 0 — a number in every case."
 },
 "producer/budget-sheet.js::fm(repM.totals.efc)": {
  "n": 1,
  "why": "Same wrapper: fm(n) = SBBudget.fmtMoney(n) (js/budget-engine.js:775-781). Even though repM comes from CMoney.costReport in another module, fmtMoney rounds the value first, so every return path is '$' plus a number and literals."
 },
 "producer/budget-sheet.js::fm(tot.subtotal)": {
  "n": 1,
  "why": "fm(n) (producer/budget-sheet.js:218) is a one-line wrapper for SBBudget.fmtMoney, which I read at js/budget-engine.js:775-781: it does n = Math.max(0, Math.round(n)) and every return path is '$' + a number or a .toFixed() string + a literal suffix, so the return value cannot contain markup no matter what is passed in."
 },
 "producer/incentives.js::fm(r.net)": {
  "n": 1,
  "why": "fm(n) (producer/incentives.js:15) is a one-line wrapper for SBBudget.fmtMoney (js/budget-engine.js:775-781), which rounds its argument and returns '$' plus a number or .toFixed() string plus a literal suffix on every path. r.net is also plain arithmetic (budget - recMid, producer/incentives.js:27)."
 },
 "producer/index.html::name": {
  "n": 1,
  "why": "Not an HTML sink. The value is interpolated into a CSS selector passed to document.querySelector('.ps-tab[data-tab=\"'+name+'\"]') at producer/index.html:190 and never reaches innerHTML; a hostile value would throw a selector SyntaxError, not execute. Both call sites pass one of four literal tab names: b.dataset.tab from the static .ps-tab buttons (line 201) and location.hash only after an indexOf(['budget','schedule','incentives','sales']) whitelist check (lines 207, 210). HTML-escaping it would corrupt the selector without adding protection."
 },
 "producer/sales-forecast.js::r[1](w)": {
  "n": 1,
  "why": "r is an element of the literal array declared inline at sales-forecast.js:263-273; r[1] is one of exactly nine formatter functions written literally in that array, and I read every one. Each now returns only literals plus esc(fm(...)) output, and two of them ('= Lifetime revenue' and the NET row path) return intentional <b> markup, so escaping the call would double-escape and show literal tags to the user. No attacker-controlled value reaches this call unescaped."
 },
 "producer/schedule-board.js::cells.join('</td><td>')": {
  "n": 1,
  "why": "SAFE. row() is a <tr>/<td> shell over cells its callers have already escaped: every cell handed to it in callSheetHtml is esc(...) output, a string literal typed in this file, or a concatenation of both. The helper itself introduces no value. Held at 1 — a second occurrence means somebody wrote a second row builder, which is the event worth failing on. Verified by reading all five call sites in producer/schedule-board.js."
 },
 "producer/schedule-board.js::k": {
  "n": 1,
  "why": "The one remaining hit (schedule-board.js:291) is wrap.querySelector('input[data-tag=\"' + k + '\"]') — a CSS selector passed to querySelector, not an HTML sink. k iterates TAG_KEYS, the module-level literal array ['stunts','sfx','vfx','water','animals','vehicles'] defined at line 39 of this same file, so it can never be attacker-controlled. HTML-escaping a selector would be the wrong encoding anyway. The genuine HTML use of the same k (line 257, data-tag attribute) is now wrapped in esc()."
 },
 "producer/schedule-board.js::tagBoxes": {
  "n": 1,
  "why": "tagBoxes is an HTML fragment assembled 14 lines above (schedule-board.js:256-259) from string literals plus esc(k), a literal-picked ' checked', and k.charAt(0).toUpperCase()+k.slice(1) where k iterates TAG_KEYS — a module-level array of literal strings defined at line 39 of this same file. It is intentional <label>/<input> markup; escaping it would double-escape the checkboxes and break the breakdown editor. Every attacker-reachable value inside it is already escaped."
 },
 "taxcredit/index.html::deltaCls": {
  "n": 1,
  "why": "Assigned one line above the sink (line 186): \"var deltaCls = model.delta < 0 ? 'tc-neg' : 'tc-pos';\". Its only two possible values are class-name literals written in this file; no external data reaches it. It is already inside a double-quoted class attribute."
 },
 "taxcredit/index.html::fm(model.advisorModel)": {
  "n": 1,
  "why": "Same fm() numeric formatter defined at line 105 of this file — Math.round() forces a Number primitive and toLocaleString() cannot emit markup characters. model.advisorModel is Math.round(bt * qp * mid) in lib-taxcred.js line 120."
 },
 "taxcredit/index.html::fm(model.budgetCap)": {
  "n": 1,
  "why": "Same fm() numeric formatter defined at line 105 of this file. Even though model.budgetCap originates from the JURIS table, fm() runs it through Math.round() first, which yields a Number primitive (or NaN) in every case, so toLocaleString() can only produce numeric text."
 },
 "taxcredit/index.html::fm(model.estCredit)": {
  "n": 1,
  "why": "Same fm() numeric formatter defined at line 105 of this file — sole return path is '$' + Math.round(n).toLocaleString(), which cannot emit < > & \" '. model.estCredit is itself computed as 0 or Math.round(rawCredit) in lib-taxcred.js line 119."
 },
 "taxcredit/index.html::fm(model.qualifiedSpend)": {
  "n": 1,
  "why": "Same fm() numeric formatter defined at line 105 of this file; its single return path is '$' + Math.round(n).toLocaleString(), which cannot produce markup characters for any input. The value itself is also Math.round()ed in T.creditModel (lib-taxcred.js line 124) before it ever reaches here."
 },
 "taxcredit/index.html::fm(model.totalSpend)": {
  "n": 1,
  "why": "Same fm() numeric formatter defined at line 105 of this file — '$' + Math.round(n).toLocaleString() cannot carry markup for any input. model.totalSpend is Math.round(totalSpend) in lib-taxcred.js line 124."
 },
 "taxcredit/index.html::fm(r.amount)": {
  "n": 1,
  "why": "fm() is defined in this same file (line 105) as \"'$' + Math.round(n).toLocaleString()\". Math.round always returns a Number primitive regardless of input, and Number.prototype.toLocaleString can only emit digits, group/decimal separators, a sign, 'NaN' or '∞' — never < > & \" '. Verified empirically against markup strings, objects, arrays, null and Infinity: output was always $-prefixed numeric text."
 },
 "timeline/timeline-budget.js::fmtMins(r.wallMinutes)": {
  "n": 1,
  "why": "fmtMins() is defined at lines 854-859 of this file; it starts with min = Math.max(1, Math.round(min)) and every return path is that number concatenated with the literals ' min', 'h ' and 'm'. Non-numeric input yields 'NaNh '. It cannot emit markup."
 },
 "timeline/timeline-budget.js::fmtMoney(bm.med)": {
  "n": 1,
  "why": "fmtMoney() (lines 846-853) reduces its argument to a number with Math.max(0, Math.round(n)) before building '$' + digits, so its output is markup-free on every path."
 },
 "timeline/timeline-budget.js::fmtMoney(prod.total.high)": {
  "n": 1,
  "why": "Same fmtMoney() at lines 846-853: numeric coercion up front, output is always '$' plus a digit string. It cannot emit a markup character."
 },
 "timeline/timeline-budget.js::fmtMoney(prod.total.likely)": {
  "n": 1,
  "why": "Same fmtMoney() at lines 846-853: the argument is numerically coerced before any string is built, so the result is always '$' plus digits."
 },
 "timeline/timeline-budget.js::fmtMoney(prod.total.low)": {
  "n": 1,
  "why": "fmtMoney() (lines 846-853) coerces its argument with Math.max(0, Math.round(n)) and returns '$' plus digits or '$NaN' on every path, so no markup can reach the DOM."
 },
 "timeline/timeline-budget.js::fmtMoney(r.highUsd)": {
  "n": 1,
  "why": "Same fmtMoney() at lines 846-853: output is always '$' plus a digit string produced by Math.round/.toFixed, or '$NaN'. Markup cannot pass through."
 },
 "timeline/timeline-budget.js::fmtMoney(r.likelyUsd)": {
  "n": 1,
  "why": "Same fmtMoney() at lines 846-853: n is coerced by Math.max(0, Math.round(n)) before use, so every return is '$' plus digits (or '$NaN'). It cannot emit < > \" ' or &."
 },
 "timeline/timeline-budget.js::fmtMoney(r.onePassUsd)": {
  "n": 1,
  "why": "fmtMoney() is defined at lines 846-853 of this file and every return path is '$' concatenated with a value that has already passed through Math.max(0, Math.round(n)) and at most .toFixed()/.replace() on digits. A non-numeric input becomes NaN, yielding the literal '$NaN'. No caller-supplied character can reach the output."
 },
 "timeline/timeline-budget.js::fmtMoney(sel.likelyUsd)": {
  "n": 1,
  "why": "Same fmtMoney() at lines 846-853: every return path is '$' plus a Math.round/.toFixed digit string, or '$NaN'. No markup character can pass through."
 },
 "timeline/timeline-characters.js::label": {
  "n": 3,
  "why": "label is the first parameter of field() (line 427). field() has exactly six call sites, lines 411-416 of this same file, and every one passes a string literal ('Description', 'Body type', 'Wardrobe', 'Voice profile', 'Default emotion', 'Cast role'). No caller-supplied data can reach it."
 },
 "timeline/timeline-characters.js::sel": {
  "n": 1,
  "why": "sel is assigned six lines above the sink, at line 393: `const sel = selected === n ? ' selected' : '';`. Both branches are string literals in this file, so the value interpolated into class=\"char-card...\" is always ' selected' or ''."
 },
 "timeline/timeline-characters.js::thumb": {
  "n": 1,
  "why": "thumb is deliberate markup built at line 390 in the same function: `c.refUrl ? '<img src=\"' + esc(c.refUrl) + '\" alt=\"\">' : '<span class=\"ph\">👤</span>'`. Its only variable part, c.refUrl, is already inside a double-quoted src and passed through esc(), which I fixed to escape both quote characters. Escaping thumb itself would render the tags as visible text and break the card layout."
 },
 "timeline/timeline-locations.js::sel": {
  "n": 1,
  "why": "sel is assigned at line 211, six lines above the sink: `const sel = selectedKey === loc.key ? ' selected' : '';`. Both branches are string literals in this file, so only ' selected' or '' reaches class=\"loc-card...\"."
 },
 "timeline/timeline-locations.js::thumb": {
  "n": 1,
  "why": "thumb is deliberate markup built at lines 212-214 of the same function: `loc.plateUrl ? '<img src=\"' + esc(loc.plateUrl) + '\" alt=\"\">' : '<span class=\"ph\">📍</span>'`. The only variable part is inside a double-quoted src and already escaped by esc(), which I fixed to cover both quote characters. Escaping thumb would print the tags as literal text."
 },
 "timeline/timeline.js::label": {
  "n": 1,
  "why": "label is the third parameter of mkTog() (line 1187). mkTog() has exactly twelve call sites, all on lines 1178-1180 of this same file, and every one passes a string literal ('Location', 'Time', 'Weather', 'Season', 'Angle', 'Film grade', 'Color', 'Saturation', 'Lighting', 'Mood', 'FX', 'Sound')."
 },
 "today/index.html::castNames.map(esc).join(' · ')": {
  "n": 1,
  "why": "Every element is already passed through this file's own esc() (today/index.html line 62, which escapes all five of & < > \" '); .map(esc) applies it per name and the ' · ' separator is a literal in this file. The scanner only fails to see it because the escaping call is not the head of the expression. Wrapping the joined string again would double-escape (a name containing & would render as &amp;), which the brief forbids."
 },
 "today/index.html::locs.map(esc).join('<br>')": {
  "n": 1,
  "why": "Same shape: each location name is escaped element-wise by this file's esc(), and the only markup in the result is the literal '<br>' separator written in this file, not attacker text. It cannot be wrapped in esc() as a whole because that would escape the intended <br> and print it as visible text."
 },
 "tools/lib-media.js::xmlEsc(e.path)": {
  "n": 1,
  "why": "Already wrapped by xmlEsc (now all five); unrecognised name only. Verified with a hostile path containing \" > < script tags and an apostrophe — fully entity-encoded on write and byte-identical after parseManifest/xmlUnesc."
 },
 "tools/lib-media.js::xmlEsc(e.sha256)": {
  "n": 1,
  "why": "Wrapped by xmlEsc (now all five). Flagged only because the scanner does not know that helper name. Hex digests are unaffected by escaping, and parseManifest's [0-9a-f]+ match still succeeds."
 },
 "tools/lib-media.js::xmlEsc(e.size || 0)": {
  "n": 1,
  "why": "Wrapped by xmlEsc (now all five). Flagged only because the scanner does not know that helper name. e.size can be any value from a shared archive, so it is escaped rather than trusted as a number."
 },
 "tools/lib-media.js::xmlEsc(meta.created || new Date().toISOString())": {
  "n": 1,
  "why": "Wrapped by this file's xmlEsc, which I fixed to escape all five of & < > \" '. The scanner's SAFE_CALL regex only knows the names esc/escAttr/escape/escapeHtml/htmlEsc/encodeURIComponent/csvSafe, so it does not recognise xmlEsc — the value itself is escaped."
 },
 "tools/lib-media.js::xmlEsc(meta.project)": {
  "n": 1,
  "why": "Already wrapped by xmlEsc (now all five of & < > \" '); flagged only because the scanner's SAFE_CALL list does not contain the name xmlEsc. Verified: a project name of O'Brien & <Co> emits O&#39;Brien &amp; &lt;Co&gt;."
 },
 "tools/tools-media-ui.js::opts": {
  "n": 1,
  "why": "`opts` is not a value, it is an HTML fragment built 6 lines above in the same function: Object.keys(MD.SENSORS).map(...) joining '<option value=\"' + escAttr(k) + '\">' + esc(MD.SENSORS[k].label) + '</option>'. Both interpolations inside it are escaped (I added the escAttr on k in this pass), so no attacker text reaches markup unescaped. Escaping `opts` itself would encode the <option> tags and render them as literal text, destroying the sensor <select>."
 },
 "tools/tools-media-ui.js::v.missing.map(esc).join(', ') : '')": {
  "n": 1,
  "why": "Every element is already escaped individually by .map(esc) before the join; the scanner's regex swallowed the trailing ternary (` : '')`) into the captured expression, which is why it does not match the safe-call prefix. The joined string lands in text content inside <div class=\"tk-result\">, never in an attribute, so esc's coverage of & < > \" is sufficient there (the missing apostrophe only matters in a single-quoted attribute). Wrapping the whole expression again would double-encode, turning a path containing & into &amp;amp;. Values are manifest paths from MD.verifyAgainst -> parseManifest -> xmlUnesc, so they are attacker-controllable and do need the per-element esc that is already present."
 },
 "vfx/index.html::fin": {
  "n": 1,
  "why": "`fin` is a local var declared 4 lines above the sink and assigned only from a 3-branch ternary whose branches are literal HTML written in this file: `<span class=\"vx-note\">—</span>` when the shot has no joined PO, a `<span title=...>` carrying esc(money(poRow.amount)) once that PO is paid, and an `<input data-final=\"' + esc(sh.id) + '\" type=\"number\">` while it is open. It is intentionally markup — escaping it would render a visible <input> tag instead of an input. Both interpolations inside it are esc()'d at the point of use: sh.id is generated by CVfx.makeShot's uid(), and poRow.amount goes through money() -> CMoneyMath.fmt, which builds its output from digits and separators only. Neither branch interpolates a vendor, description or any other free-text field."
 },
 "vfx/index.html::verdict": {
  "n": 1,
  "why": "`verdict` is a local var declared 3 lines above the sink and assigned only from a 4-branch ternary whose every branch is a literal HTML string written in this file (`<span class=\"vx-tag\">no bid yet</span>`, `<span class=\"vx-under\">within range</span>`, and two `<span class=\"vx-over\">/<span class=\"vx-under\">` branches). It is intentionally markup, so escaping it would render visible tags. Its only interpolations are money(c.delta) and money(-c.delta), which are wrapped in esc(); money() now delegates to CMoneyMath.fmt (it was `'$' + (+n || 0).toLocaleString()` when this entry was written, and printed a $12,000.50 bid as `$12,000.5`), which coerces through CMoneyMath.cents and emits digits, commas, a dot and a leading -$ only — never attacker text. No branch reads localStorage or shot fields."
 },
 "wardrobe/index.html::pieceRows(l)": {
  "n": 1,
  "why": "`pieceRows` is defined in this same file at wardrobe/index.html:181-189 and returns HTML by design (the <tr> rows of the piece table), so escaping it would render visible tags. After this pass every value it interpolates is escaped: esc(i) for the loop index in all four data-i/data-delpiece attributes, esc(p.item) for the item name, esc(p.cost || 0) for the cost, and esc(s) for the <option> text drawn from W.SOURCES. Everything else in its return value is a string literal written in this file, so it cannot carry attacker markup out to the caller."
 },
 "workflow/advisor-ui.js::s[0]": {
  "n": 1,
  "why": "s is `SEV[x.sev] || SEV.low` (line 58). SEV is a module-level constant defined literally at advisor-ui.js:21 with four entries whose values are literal arrays of the form ['#RRGGBB','LABEL']; nothing in the file ever writes to SEV. So s[0] is one of four literal hex colours — and if x.sev happens to name an inherited Object.prototype member, s[0] is undefined and stringifies to \"undefined\". Neither can carry markup into the style attribute. (x.sev itself is only used as a lookup key and is never interpolated.)"
 },
 "workflow/workflow.js::c.status": {
  "n": 1,
  "why": "c iterates a.clips, which assess() builds in this same file (lines 176-181). Each element's status is `c.status === 'approved' ? 'approved' : c.status === 'generating' ? 'generating' : (c.videoUrl ? 'rendered' : 'queued')` — the stored clip status is only compared, never passed through, so the rendered value is always one of four in-file literals."
 },
 "workflow/workflow.js::k": {
  "n": 1,
  "why": "k is the loop variable of `['generating', 'queued', 'rendered', 'approved'].map(function (k) {...})` — an array literal written inline at workflow.js:258. It can only ever be one of those four literal strings from this file."
 },
 "workflow/workflow.js::st.status": {
  "n": 2,
  "why": "st comes from assess() in this same file, which render() calls directly (line 222). Every stage's status is written from a string literal in this file: the seven `status:` properties (lines 42, 59, 79, 100, 117, 135, 156) are ternaries between 'done', 'active' and 'todo', and line 166 assigns 'active'. A clip's or store's own stored status is only ever compared with ===, never copied into st.status, so this value is always one of three in-file literals. Covers both flagged lines, 233 and 238."
 }
}
